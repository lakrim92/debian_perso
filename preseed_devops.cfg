#### Localisation
d-i debian-installer/locale string fr_FR.UTF-8
d-i localechooser/supported-locales multiselect fr_FR.UTF-8, en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select fr
d-i keyboard-configuration/variant select

#### Réseau
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-devops
d-i netcfg/get_domain string local
d-i netcfg/wireless_wep string

#### Miroir Debian
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string
d-i mirror/suite string bookworm

#### Horloge
d-i clock-setup/utc boolean true
d-i time/zone string Europe/Paris
d-i clock-setup/ntp boolean true

#### Partitionnement automatique (LVM, disque entier)
d-i partman-auto/method string lvm
d-i partman-auto-lvm/guided_size string max
d-i partman-auto/choose_recipe select atomic
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

#### Utilisateur
# Mot de passe par défaut : debian — modifier avant de générer l'ISO
d-i passwd/root-login boolean false
d-i passwd/user-fullname string BelLinuxien
d-i passwd/username string bellinuxien
d-i passwd/user-password password debian
d-i passwd/user-password-again password debian
d-i passwd/user-default-groups string audio cdrom dip floppy sudo video plugdev netdev

#### Sélection des paquets
tasksel tasksel/first multiselect standard, gnome-desktop
d-i pkgsel/include string curl wget git sudo
d-i pkgsel/upgrade select full-upgrade
popularity-contest popularity-contest/participate boolean false

#### Bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

#### Script post-installation
d-i preseed/late_command string \
    cp /cdrom/post_install.sh /target/tmp/post_install.sh ; \
    in-target chmod +x /tmp/post_install.sh ; \
    in-target bash /tmp/post_install.sh 2>&1 | tee /target/var/log/post-install.log

d-i finish-install/reboot_in_progress note
